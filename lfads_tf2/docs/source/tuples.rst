##########
Tuples
##########

We use tuples to organize `tf.Tensor`s so we don't have to pass around 
large numbers of arguments. The `collections.namedtuple` provides 
convenient utilities for naming the elements of these tuples so 
they can be accessed as properties.

.. automodule:: lfads_tf2.tuples
    :members: