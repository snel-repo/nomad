##########
Defaults
##########

The defaults module creates the default configuration object that we use 
to set up the LFADS model. We use the `yacs` library to handle setting 
defaults and updating the configuration.

.. automodule:: lfads_tf2.defaults
    :members: